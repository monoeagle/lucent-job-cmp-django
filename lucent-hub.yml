# MPP Marketplace Portal (Django) — Lucent Hub App Registration

name: mpp-Django
display_name: "MPP Django"
description: "Marketplace Portal — Self-Service IT-Provisioning (Django 6.0 + Tailwind)"
version: "1.0.0"

port: 8000
docs_port: 5063
health_endpoint: /health
theme_color: "#092E20"
category: "portale"

setup:
  command: bash scripts/run.sh setup
  label: "Setup (venv + DB + migrate + seed)"

tags:
  - portal
  - django
  - python
