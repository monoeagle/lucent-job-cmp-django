# CloudMan Portal (Django) — Lucent Hub App Registration

name: cmp-Django
display_name: "CloudMan Portal"
description: "CloudMan Portal — Self-Service IT-Provisioning (Django 6.0 + Tailwind)"
version: "1.3.1"

port: 8000
docs_port: 5063
health_endpoint: /health
theme_color: "#092E20"
category: "portale"

setup:
  command: bash scripts/run.sh setup
  label: "Setup (venv + DB + migrate + seed)"

tags:
  - portal
  - django
  - python
