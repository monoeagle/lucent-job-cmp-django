# Session Handoff — 2026-07-17 22:59 (CloudMan Portal v1.3.0)

## Was wir gemacht haben
Aus einer **VM-Rückmeldung** mit 6 Punkten wurde ein komplettes Rename- und
Feature-Release. Der Nutzer hatte `deploy/install.sh` erstmals auf einer echten
AlmaLinux-9-VM laufen lassen und gemeldet: Portal aus dem Subnetz nicht
erreichbar (nginx lief, aber kein Zertifikat → nur HTTP), Redis musste
nachinstalliert werden, „Version unbekannt" im Panel, Doku als AppImage am
Windows-Terminal-Server nicht öffenbar — und: **MPP → CMP (CloudMan Portal)**
umbenennen. Alles außer den zwei VM-Verifikationen ist erledigt, **Release
v1.3.0** nach `main` gemergt und gepusht.

## Erledigt (8 Commits, `65f430c..cfdaefa`, auf `main` gepusht)
- [x] **Code-Rename MPP→CMP** (`b37a37a`): Container `mpp/`→`cmp/` (git mv, 390
      Renames, History erhalten), 31 Bash-Funktionen `cmp_*`, 8 Vars `CMP_*`,
      System-Identifier (`/opt/cmp`, `/etc/cmp/cmp.env`, User `cmp`, DB `cmp_prod`,
      Services `cmp-web`/`cmp-celery`), Test/Dev-DB `cmp_django_test/dev` (Rolle
      `cmp` lokal via `sudo -u postgres` angelegt — **Nutzer musste den einen
      psql-Befehl selbst ausführen**, sudo braucht TTY), templatetags `cmp_tags`,
      Celery-App `cmp`. Django-App-Code war importfrei von `mpp`.
- [x] **HTTP/HTTPS-Auto-Modus** (`97a98bf`, TDD, 9 Tests): Zert für FQDN da →
      HTTPS, sonst HTTP (kein self-signed mehr). `production.py`:
      `SESSION/CSRF_COOKIE_SECURE` env-steuerbar (waren hart True → Login über
      HTTP unmöglich). `cmp_render_nginx`/`cmp_env_security_lines`/
      `cmp_portal_proto` in `lib.sh`; Panel zeigt echtes Protokoll/Port.
- [x] **Redis air-gapped** (`963ba75`, TDD): fehlt Redis, zieht `install.sh` es
      offline aus `rpms/redis*.rpm` (`dnf --disablerepo='*'`); `build_release.py`
      nimmt `rpms/` optional ins Bundle (auf Staging-Host befüllt).
- [x] **Doku als HTML-ZIP** (`8ad8b25`): `./run.sh docs-zip` →
      `release/Lucent-CMP-Django-Docs-<v>-html.zip` (entpacken, index.html öffnen).
      Gate-Regel **R-APPIMAGE → R-DOCS-ZIP**; AppImage bleibt optionale Linux-Variante.
- [x] **Branding + Doku-Site** (`fe594ff`, `e396b4c`): Templates/lucent-hub/README/
      CLAUDE.md/`.claude`/`scripts` (`mpp.sh`→`cmp.sh`, `fix_databases.sh`
      Flask-Teil geschützt); Doku-Site `mpp-docs/`→`cmp-docs/`, Deployment-Guides
      + **HTTP-Modus-Doku + Netzwerk-Prüfbefehle** (ss/firewall/curl + PowerShell).
- [x] **Release v1.3.0** (`db4516c`): Version synchron (lucent-hub/zensical/
      icon-rail; run.sh+build_release leiten ab), Changelog, Kennzahlen (Tests
      238→328 Alt-Drift in `ueberblick.md`). Doku-Gate **12/12 grün**.
- [x] **v1.3.0-Nachtrag** (`cfdaefa`): `seed.py` + `todo-erledigt.md`-Stand-Zeile
      hingen noch auf v1.1.0 — **R-VERSION prüft diese Stellen nicht** (Memory-Fund).
- [x] **Memory aktualisiert**: `cmp`-Pfade, neue Fakten (Rename-Grenzen, HTML-ZIP,
      R-DOCS-ZIP), obsolete AppImage-Frische-Lücke ersetzt.

## Offen / Nächste Schritte
- [ ] **gh-pages deployen** — Live-Doku hängt auf v1.1.0; lokal ist v1.3.0/328
      gebaut. Ablauf: voller Build + `project-activity.json` committen (`deploy_ghpages.sh`
      baut mit `--no-activity`), dann `cd cmp-docs && ./deploy_ghpages.sh`, danach
      `curl -s https://monoeagle.github.io/lucent-job-MPP_Django/javascripts/icon-rail.js | grep APP_VERSION` → muss `1.3.0` zeigen. **Diese Session als „leg los" freigegeben — läuft ggf. schon.**
- [ ] **VM-Verifikation (Punkt 5 + Installer):** `ls -l /opt/cmp/app/VERSION` beim
      nächsten Bundle-Test klärt „Version unbekannt". Und: HTTP-Modus + Redis-RPM +
      PostgreSQL-Erkennung sind weiterhin **nur per Fakes getestet**, nie auf echter
      AlmaLinux-9-Hardware. Vorgehen: `./run.sh release` → Bundle auf VM →
      `sudo ./deploy/install.sh --check`, dann `--install` bzw. `--with-packages`.
- [ ] **GitHub-Repo-Rename** (`lucent-job-MPP_Django` → `cmp-…`): reine Nutzer-Aktion.
      Danach die geschützten Repo-URLs in Doku/Handoffs nachziehen.
- [ ] Danach: **AP-11** (Podman/Quadlets, ADR-0001) oder **AP-12** (Django Channels).

## Entscheidungen dieser Session
- **Test/Dev-DB mit umbenannt** (Nutzer-Wahl „voll konsistent") — reale lokale
  `cmp`-Rolle + DBs angelegt, statt die lokale Infra bei `mpp` zu belassen.
- **Kein Migrationsskript** für Bestandsinstallationen (Nutzer-Wahl) — `cmp` wird
  frisch aufgesetzt, nicht aus `mpp` migriert.
- **Branding-Scope „Alles außer History"** — inkl. `.claude/`-Tooling, aber
  historische Docs + Repo-URLs + `mpp-TDD` geschützt.
- **v1.3.0 als MINOR** (Policy: autonom) — nur so wird die Doku konsistent (Code
  war schon cmp + 328, Release stand auf v1.2.0/317).
- **HTTP-Auto ohne self-signed** (Nutzer-Wahl) — fehlt ein Zert, HTTP + laute
  Warnung; wer HTTPS will, spielt ein Zert unter `/etc/pki/cmp/` ein.

## Git-Status
- Branch `main` == `origin/main` (`cfdaefa`), 8 Session-Commits gepusht.
- Feature-Branch `rename/cloudman-portal` existiert lokal + remote (kann gelöscht
  werden — via FF in `main`).
- **Kein Tag/GitHub-Release** für v1.3.0 erstellt (nur gemergt). Bei Bedarf: taggen.
- Tests **328 grün**, Doku-Gate **12/12 grün**.
- Remote-URL zeigt auf alten Namen `MPP_Django` (Redirect greift).

## Hinweise
- venv-Shebangs kaputt → Tools via `venv/bin/python3 -m …`; Docs:
  `cmp-docs/.venv-docs/bin/python3 build_docs.py` (funktioniert nach `git mv`).
- `pytest-cov` **nicht** installiert — keine Coverage-Kennzahl.
- **R-VERSION prüft nur 4 Stellen** — `seed.py` (`system_startup`) +
  `todo-erledigt.md`-Stand-Zeile manuell mitziehen (waren schon 2× vergessen).
- HTML-ZIP nach Doku-Änderung neu: `./run.sh docs-zip` (nach dem Site-Build).
- gh-pages-Deploy braucht explizite Nutzer-Freigabe.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit **CloudMan Portal (CMP)** — vormals MPP Django (Django 6 + HTMX, TDD).
Letzte Session 2026-07-17: kompletter Rename MPP→CMP + Installer-HTTP/HTTPS-Auto-Modus
+ Redis-air-gapped + Doku-HTML-ZIP, **Release v1.3.0** nach main gemergt+gepusht
(317→328 Tests, Doku-Gate 12/12 grün).
Kontext: `deploy/install.sh` wählt HTTP/HTTPS automatisch (Zert da→HTTPS, sonst HTTP),
Redis wird notfalls offline aus `rpms/` gezogen. Alles per Fakes getestet — **nie auf
echter AlmaLinux-9-VM gelaufen**. `mpp-TDD` ist das Flask-Schwesterprojekt (nicht anfassen),
Repo heißt real noch `lucent-job-MPP_Django`.
Nächster Schritt: (1) gh-pages auf v1.3.0 ziehen (hängt auf v1.1.0) — voller Build +
project-activity.json committen, dann `cd cmp-docs && ./deploy_ghpages.sh`, live per curl
prüfen. (2) `install.sh` auf echter Alma-9-VM end-to-end + `ls /opt/cmp/app/VERSION`
(klärt „Version unbekannt"). (3) GitHub-Repo-Rename (Nutzer). Alternativ AP-11 (Podman)
oder AP-12 (Channels).
Handoff-Datei: docs/handoffs/2026-07-17-2259.md
