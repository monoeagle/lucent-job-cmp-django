# Session Handoff — 2026-07-16 20:17

## Was wir gemacht haben
Aus „schau dir das letzte Handoff an" wurde eine **Installer-Session**. Im
Arbeitsbaum lagen uncommittete Reste einer Session vom 15.07. (Architektur-
Vergleich-Seite, ein `BUNDLE_DIR`-Fix). Die Frage „läuft `install.sh`
idempotent?" ergab: nein — und beim Testbarmachen fielen 11 Installer-Bugs und
5 Doku-/Gate-Drifts auf. Der Installer ist jetzt idempotent, erkennt PostgreSQL
in beiden Varianten (PGDG/AppStream), hat ein Menü mit Prüfbereich und ist mit
**78 Unit-Tests** abgedeckt. Tests **239 → 317**, Version bleibt **v1.1.0**.

## Erledigt (6 Commits, `ed64dad..f257588`, gepusht + gh-pages live)
- [x] **Idempotenz** (`deploy/lib.sh`, unit-getestet): SECRET_KEY wird aus
      `mpp.env` übernommen statt neu gewürfelt (warf sonst alle Nutzer raus);
      Rolle **und** Datenbank getrennt geprüft (Wiederanlauf nach Teilfehler legte
      die DB nie an); App wird **gespiegelt** statt gemerged (gelöschte Module +
      alte Migrationen überlebten); **`restart`** statt `enable --now` (der stille
      Upgrade-Fehler: alter Code lief weiter, Skript meldete Erfolg); Env-Übergabe
      NUL-getrennt (Passwort mit Leerzeichen zerfiel in mehrere Argumente).
- [x] **PostgreSQL-Erkennung PGDG + AppStream**: psql-Pfad (PGDG legt es nach
      `/usr/pgsql-16/bin/`, **nicht** in den PATH), Service-Name, Datenverzeichnis,
      `initdb`-Variante. `postgresql-16.service` war hart verdrahtet; die Doku
      forderte `Requires=`, das Skript schrieb nur `After=`.
- [x] **`--with-packages`** (Online-Modus, **kein** Default): PGDG + EPEL,
      AppStream-Modul **vor** der Installation deaktivieren, `initdb` nur bei
      fehlendem Cluster (`PG_VERSION`-Prüfung).
- [x] **nginx-Preflight + `--skip-nginx`**: nginx wurde nie geprüft — fehlte es,
      lief der Installer komplett durch und starb am Ende an `/etc/nginx/conf.d/`.
      **Redis** wird gestartet statt bemängelt (fehlt es → Abbruch, Celery braucht
      den Broker).
- [x] **Menü mit Prüfbereich + Links/Ports** (`deploy/ui.sh`): Erhebung und
      Rendering strikt getrennt (Daten über stdin) → gegen erfundene Zustände
      testbar. Aktionen `--install` / `--check` / `--restart`; **ohne TTY kein
      Menü**, dann direkte Installation (bestehende Automatisierung bleibt gültig).
      `--check` liefert **Exit 0 nur bei durchgehend grün** → Health-Check.
- [x] **`VERSION` ins Release-Bundle** (`tools/build_release.py`) — die Version
      lag nur in `lucent-hub.yml` (nicht im Bundle) und als Fließtext in
      `START-HIER.txt`, war auf der VM also nicht maschinenlesbar.
- [x] **`R-STALE` repariert**: erhob die Wahrheit nicht, sondern suchte fossile
      Konstanten (`228|244`) in nur `*.html`. Belegt per Mutation: mit
      `999 Tests grün` im HTML **und** `TEST_COUNT=999` im JS meldete das alte Gate
      „✓ keine Alt-Zahlen". Jetzt: `pytest --collect-only` als Wahrheit, Abgleich
      gegen `icon-rail.js`, Scan über HTML **und** JS, Fehlschlag statt
      Durchwinken wenn die Zahl nicht ermittelbar ist.
- [x] **Doku-Drift behoben** (grep-belegt, nicht behauptet): Header-Badge zeigte
      hartkodiert „239 Tests", Info-Tabelle „230" (widersprüchlich, beide alt) →
      eine `TEST_COUNT`-Konstante; `testing.md` führte seit **19.06.** „238 Tests"
      samt veralteter Aufteilung → frisch erhoben (Unit 215 / Integration 98 /
      E2E 4 / gesamt 317); `projektstruktur.md` kannte weder `deploy/` noch
      `tools/` noch `requirements/production.txt` (Drift seit v1.1.0, 27.06.).
- [x] **Architektur-Vergleich-Seite** (SSR vs. API-First) aus der Session vom
      15.07. committet.
- [x] Site neu gebaut, **Docs-AppImage** neu (3 Kopien byte-gleich, Inhalt durch
      Extrahieren verifiziert), **gh-pages deployt und live per `curl` geprüft**.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (seit Session 3 unverändert, jetzt aber billig):**
      `deploy/install.sh` auf einer **echten AlmaLinux-9-Test-VM** end-to-end.
      Die 78 Tests beweisen, dass die Logik die richtigen Kommandos in der
      richtigen Reihenfolge wählt — **nicht**, dass echtes `dnf`, systemd, PGDG
      und SELinux mitspielen. Vorgehen: Bundle bauen (`./run.sh release`), auf die
      VM, dann `sudo ./deploy/install.sh --check` (sagt in einem Aufruf, was
      fehlt), danach `--with-packages` (Repos sind laut Nutzer freigeschaltet)
      bzw. der Offline-Weg. Besonders prüfen: ob PGDG `psql` wirklich aus dem PATH
      hält (die Erkennung fängt beide Fälle ab, aber verifiziert ist es nicht) und
      ob die `Requires=`-Unit gegen den erkannten Service sauber hochkommt.
- [ ] Danach: **AP-11** (Podman/Quadlets, ADR-0001) oder **AP-12** (Channels,
      Schritte in `todo.md`).
- [ ] Optional offen: unbeaufsichtigte Installation (Konfiguration per Parameter
      statt interaktiver Eingaben + `trap`-Fehlerbehandlung). **Nicht** umgesetzt:
      der ChatGPT-Vorschlag, `createsuperuser` durch ein Konto mit fest
      hinterlegtem Passwort zu ersetzen — das wäre ein bekanntes Default-Admin-
      Passwort auf jeder Installation, im Skript und in den Logs. Falls gewünscht:
      Passwort aus Umgebungsvariable oder generiert + einmalig ausgegeben.
- [ ] Optional: `install.sh` (237 Z.) in Funktionen weiter aufteilen; `--check`
      lockern, falls Warnungen (z.B. „Version unbekannt") zu streng rot machen.

## Entscheidungen dieser Session
- **Kein `rsync`** für die Spiegelung (`rm -rf` + `cp -a` stattdessen): rsync ist
  auf einer minimalen AlmaLinux nicht garantiert vorhanden — für einen
  air-gapped Installer keine akzeptable Abhängigkeit. Preis: es wird jedes Mal
  alles neu kopiert statt inkrementell.
- **`--with-packages` als Opt-in, nicht als Default**: das Bundle muss air-gapped
  funktionieren; diese Zusage darf nicht von freigeschalteten Repos abhängen.
- **Beide PostgreSQL-Varianten erkennen statt eine raten** (Nutzer-Entscheidung).
- **Zertifikat: Rat von ChatGPT bewusst verworfen.** Der Vorschlag war, die reine
  `[ ! -f crt ]`-Prüfung zu behalten — genau der Bug: bei geändertem FQDN liefert
  nginx weiter den alten CN. Jetzt SAN-Abgleich; CA-signierte Zertifikate werden
  nie überschrieben.
- **`cmp`-Guard für systemd-Units abgelehnt**: Restart ist ohnehin nötig (App-Code
  ändert sich), und der Guard verleitet zum Fehlschluss „Unit unverändert → kein
  Restart" — das war der stille Upgrade-Bug.
- **Installer-Arbeit als EIN Commit** statt der vier angebotenen: der Arbeitsbaum
  enthielt nur den Endzustand; vier Commits hätten erfundene Zwischenstände
  bedeutet.
- **Kein Version-Bump** (dated Changelog-Einträge, Präzedenz aus Session 2/4).

## Git-Status
- Branch: `main` == `origin/main` (`f257588`), 6 Session-Commits gepusht.
- Uncommitted: Handoff/Insight/KPI-Zeile dieser Session (folgen als Commit).
- Stash: keine.
- Tests **317 grün**, Doku-Gate **grün** (inkl. strict Build), gh-pages live.

## Hinweise
- venv-Shebangs kaputt → Tools via `venv/bin/python3 -m …`; Docs-Venv:
  `mpp-docs/.venv-docs/bin/python3 build_docs.py`.
- `pytest-cov` ist **nicht** installiert — es gibt keine Coverage-Kennzahl.
- Nach Doku-Änderungen **Docs-AppImage manuell** neu bauen (`./run.sh
  docs-appimage`) **und nach `../AppImages/` kopieren** — das tut das Skript
  nicht selbst, und R-APPIMAGE prüft nur Byte-Gleichheit, nicht Frische.
- Vor gh-pages-Deploys **vollen** Build laufen lassen + `project-activity.json`
  committen (`deploy_ghpages.sh` baut mit `--no-activity`).
- gh-pages-Deploy braucht explizite Nutzer-Freigabe.
- git-Remote zeigt weiter auf den alten Namen `MPP_Django` (Redirect greift).
- Installer-Tests injizieren `MPP_PSQL` / `MPP_SYSTEMCTL` / `MPP_DNF` /
  `MPP_NGINX` / `MPP_PG_PREFIX` / `MPP_PY` — dieselben Variablen taugen zum
  manuellen Durchspielen ohne echte Dienste.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django 6 + HTMX, TDD).
Letzte Session: 2026-07-16 20:17 (Offline-Installer idempotent + PG-Erkennung + Menü; 239→317 Tests; v1.1.0 unverändert).
Kontext: `deploy/install.sh` ist jetzt wiederholt ausführbar, erkennt PostgreSQL (PGDG/AppStream) statt es zu raten, hat Prüfbereich/Menü und 78 Unit-Tests (`deploy/lib.sh`, `deploy/ui.sh`). Alles per Fakes getestet — **nie auf echter Hardware gelaufen**.
Nächster Schritt: `deploy/install.sh` auf einer echten AlmaLinux-9-Test-VM end-to-end durchspielen — Bundle via `./run.sh release`, auf der VM `sudo ./deploy/install.sh --check`, dann `--with-packages` (Repos sind freigeschaltet). Prüfen: PGDG-`psql` im PATH? `Requires=` gegen erkannten Service? Alternativ AP-11 (Podman) oder AP-12 (Channels) starten.
Handoff-Datei: docs/handoffs/2026-07-16-2017.md
