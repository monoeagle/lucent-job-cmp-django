# Session Handoff — 2026-07-21 22:02

## Was wir gemacht haben

Der Nutzer hat eine **Fremddoku** ins Repo geholt: das Bookstack-Buch des
Bestellportals auf der Zielmaschine (2819 Zeilen), konzipiert als **API-First mit
DRF**. Auftrag: analysieren, obwohl CMP der bewusste Gegenentwurf ist (**rein SSR**
— in dieser Session ausdrücklich bestätigt, es gab dazu ein Missverständnis).

Daraus wurde eine vollständige Gap-Analyse gegen den **echten CMP-Code**, das
Doku-Release **v1.3.3** mit der Analyse als interner Doku-Seite, und — ungeplant —
ein **History-Rewrite**, weil die „anonymisierte" Quelldatei noch interne Kennungen
enthielt und bereits öffentlich lag.

Kein Anwendungscode geändert. Offline-ZIP `…-1.3.1-almalinux9-offline.zip` bleibt gültig.

## Erledigt

- [x] Fremddoku vollständig gelesen + 4 Bilder ausgewertet (`django.png` = W3Schools-Lehrplan, `proto1-3` = Prototyp)
- [x] **Analyse-Report** `analyse/analyse-bestellportal.md` (693 Z.): Gap-Analyse aller
      FM_*/FK_*-Anforderungen + Domänenmodell (jede Zeile mit Beleg Datei:Zeile),
      Antworten auf alle 19 Punkte aus `anforderungen.md`, `django.png`-Bewertung,
      Prototyp-Bewertung, priorisiertes Fazit
- [x] **Anonymisierung** der Quelldatei: `<ad-domain>`, `<user-id>`, `<autor>`,
      `<admin-user>`, `<org>`, `<umgebung>`, `<ai-recherche-link>` — 7 Zeilen,
      CRLF byte-genau erhalten
- [x] **History-Rewrite** `e8e6cac` → `6c12734` + Force-Push; Sicherung auf Branch `backup-vor-rewrite`
- [x] **v1.3.3** an allen 5 Stellen + Changelog
- [x] Analyse als **interne Doku-Seite** `cmp-docs/docs/intern/analyse-bestellportal.md`,
      Nav-Abschnitt „Intern" in `zensical.toml`
- [x] **ZIP-Ausschluss** in `run.sh cmd_docs_zip`: `intern/` **und** `intern-*`-Assets
      (das gerenderte Mermaid-SVG rutschte im ersten Anlauf durch) → 62 statt 64 Dateien
- [x] `analyse/.gitignore`: Screenshots (`*.png`) + gebaute HTML bleiben lokal
- [x] Doku-Drift behoben: `projektstruktur.md` (neues `analyse/` + `docs/intern/`),
      `arbeitspakete.md` (2× v1.3.2 → v1.3.3)
- [x] Doku-Gate **12/12 grün**, 330 Tests, gh-pages deployt und **live per HTTP 200 verifiziert**
- [x] Insight `docs/insights/2026-07-21-gruene-tests-beweisen-keine-naht.md`

## In Arbeit / Nächste Schritte

- [ ] **SOFORT — Entscheidung offen:** Soll aus dem Hauptbefund **AP-13** werden?
      Der Nutzer wurde gefragt, kam nicht mehr dazu. Wenn ja: `todo.md` **und**
      `cmp-docs/docs/entwicklung/arbeitspakete.md` gemeinsam ergänzen — `R-AP-SYNC`
      vergleicht die höchste AP-Nummer beider Dateien und wird sonst rot.
- [ ] **Der Befund selbst — Workflow verdrahten (fachlich Vorrang vor allem anderen):**
      `ApprovalService.approve()` (`cmp/apps/approvals/services.py:49-72`) setzt
      `APPROVED` und endet. Niemand ruft `dispatch_provisioning`
      (`cmp/apps/provisioning/tasks.py:7`); `SubscriptionService.create_from_order`
      und die Celery-Tasks werden **nur von Tests** aufgerufen, `AuditService.log`
      und `NotificationService.create` **nur von `seed.py`**.
      → Audit-Log und Benachrichtigungen enthalten im Betrieb ausschließlich Seed-Daten.
      Abnahme über einen E2E-Test **durch die Views**, nicht durch direkte Service-Aufrufe.
- [ ] **HTMX-Fehler (klein, Vorlage vorhanden):** `AuditLogListView`
      (`cmp/apps/audit/views.py:9`) hat keinen htmx-Zweig, `audit_list.html:15-29`
      feuert aber `hx-get` auf `#audit-table` → die komplette Seite wird in ein `<div>`
      geswappt. Fix = Partial + `get_template_names()` analog `cmp/apps/catalog/views.py:25`.
- [ ] **GitHub-Support-Ticket:** Der alte Commit `e8e6cac` ist über seine SHA
      **weiterhin öffentlich abrufbar** (verifiziert per `gh api …?ref=e8e6cac`).
      Force-Push entfernt unreferenzierte Objekte nicht. Nutzer wurde gefragt, ob ich
      den Ticket-Text aufsetze — offen.
- [ ] **VM-Verifikation** (offen seit v1.3.0): `install.sh` lief nie auf echter Hardware.
      Die Idempotenz ist **behauptet** (`deploy/install.sh:23`), nicht bewiesen.
      Dabei gleich mitprüfen: zweiter Lauf sauber? Und: es gibt **keinen Abräumzweig**
      (`--uninstall`/`--purge` fehlen) — im Analyse-Report §2.1 ausgearbeitet.
- [ ] AP-11 Container (Podman/Quadlets) · AP-12 Live-Updates (Channels) — unverändert offen

## Entscheidungen dieser Session

- **CMP bleibt rein SSR.** Die Forderung „Doku muss auf API First neu aufgebaut werden"
  (`analyse/anforderungen.md:4`) gehört zum Fremdsystem. Der Report übersetzt jede
  API-First-Anforderung in ihre SSR-Entsprechung, statt sie als „passt nicht" abzutun.
- **Analyse als Unterseite statt loser HTML-Datei** — Navigation, Suche, Theme und
  site-eigenes Mermaid; eine Einzeldatei wäre eine Waise gewesen und hätte
  `mermaid.min.js` (3,2 MB) ein zweites Mal ausgeliefert.
- **Interner Bereich, nicht im Kunden-ZIP.** Der Report benennt CMP-eigene Schwächen;
  auf gh-pages sichtbar (Repo ist ohnehin öffentlich), aber nicht im Auslieferungspaket.
- **Screenshots nicht veröffentlichen.** `proto3.png` zeigt interne Standort-/DNS-/
  Proxy-Bezeichner; Bilder lassen sich nicht per `grep` prüfen. Technisch per
  `.gitignore` abgesichert, nicht nur per Vorsatz.
- **`anforderungen.md` mitveröffentlicht** — geprüft frei von Kennungen und 8× im
  Report referenziert; ohne sie liefen die Verweise ins Leere.

## Wichtig für die nächste Session

**Zwei eigene Fehler, beide nur durch Nachmessen gefunden — nicht durch Erfolgsmeldungen:**

1. Das Anonymisierungs-Skript hat **2672 CRLF-Zeilenenden** stillschweigend nach LF
   normalisiert (`read_text`/`write_text`). Der Diff zeigte 2675 statt 7 geänderter
   Zeilen. Byte-genau wiederholt mit `read_bytes`/`write_bytes`.
2. Der Mermaid-Renderer setzte `../images/` für eine **Tiefe-2-Seite**; korrekt ist
   `../../images/`. Hätte ein kaputtes Diagramm live ergeben.

Beide hatten dieselbe Form wie die Hauptbefunde: **eine Erfolgsmeldung über den
Vorgang statt über das Ergebnis.** „Build ohne Fehler", „Ersetzung lief durch",
„Force-Push erfolgreich" — dreimal wahr und dreimal nicht die Frage, die zählte.

## Git-Status

- Branch: `main` @ `9c743f2` (gepusht) — Handoff-Commit folgt
- Uncommitted: Insight + Handoff + KPI-Zeile + Doku-Drift-Fixes (projektstruktur/arbeitspakete) + Site-Rebuild
- Stash: keiner
- Zusatz-Branch: `backup-vor-rewrite` @ `797a139` (lokale Sicherung vor dem Rewrite — kann weg, sobald der Rewrite als gut befunden ist)
- gh-pages: deployt, live verifiziert:
  <https://monoeagle.github.io/lucent-job-cmp-django/intern/analyse-bestellportal/>
- **Kein Tag `v1.3.3` gesetzt** — bewusst offen gelassen, da die AP-13-Entscheidung
  noch aussteht und ggf. gleich mit hineinsollte.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit CloudMan Portal (CMP-Django).
Letzte Session: 2026-07-21 22:02
Kontext: v1.3.3 ist ein reines Doku-Release — Analyse einer Fremddoku (Bestellportal
der Zielumgebung, API-First/DRF) gegen CMP (rein SSR), veröffentlicht als interne
Doku-Seite unter "Intern" (nicht im Kunden-ZIP). Doku-Gate 12/12 grün, 330 Tests.
Zuletzt erledigt: Anonymisierung der Fremddoku + History-Rewrite (der alte Commit
e8e6cac ist über seine SHA aber weiterhin öffentlich abrufbar), gh-pages live.
Nächster Schritt: Entscheiden, ob der Hauptbefund als AP-13 angelegt wird — der
Workflow hinter der Genehmigung ist NICHT verdrahtet: ApprovalService.approve()
(cmp/apps/approvals/services.py:49) setzt APPROVED und endet; dispatch_provisioning
(cmp/apps/provisioning/tasks.py:7) ruft niemand, SubscriptionService.create_from_order
nur Tests, AuditService.log und NotificationService.create nur seed.py. Folge:
Audit-Log und Benachrichtigungen enthalten im Betrieb nur Seed-Daten. Bei AP-Anlage
todo.md UND cmp-docs/docs/entwicklung/arbeitspakete.md gemeinsam pflegen (R-AP-SYNC).
Handoff-Datei: docs/handoffs/2026-07-21-2202.md
