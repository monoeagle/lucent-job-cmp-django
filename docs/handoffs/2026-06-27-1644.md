# Session Handoff — 2026-06-27 16:44

> Aktualisiert; löst `2026-06-27-1454.md` ab (gleiche Session, weitergearbeitet).

## Was wir gemacht haben
Doku-/Release-Politur des MPP-Portals nach dem Vorbild des Schwesterprojekts
**luDBxP**: Oberflächen-Galerie + gh-pages-Veröffentlichung, Startseite/Architektur
im luDBxP-Stil, Bestell-Bugfix, **Offline-Release für AlmaLinux 9** (gebündelte
Wheels + Installer, als GitHub-Release veröffentlicht) und eine **ADR** zur
Deployment-Strategie. Version **v1.1.0**.

## Erledigt (Session 3, 16 Commits)
- [x] **Bugfix** `fix(orders)`: Pflicht-Parameter `location` nicht mehr als Kontext-Key
      weggefiltert. TDD, **238 → 239** Tests.
- [x] **Oberflächen-Galerie** (`referenz/oberflaeche.md`): 13 Selenium-Screenshots
      (Requester/Approver/Admin), Lightbox. **Bestellformular leer + ausgefüllt
      side-by-side** — beide **gleich groß** (1280×1209, via CDP-Viewport),
      links rote Pflichtfeld-Rahmen + leere Zusammenfassung, rechts **volle**
      Zusammenfassung.
- [x] **gh-pages live**: Repo public, Pages aktiv, `mpp-docs/deploy_ghpages.sh`.
      https://monoeagle.github.io/lucent-job-MPP_Django/
- [x] **Startseite/Architektur** an luDBxP angeglichen (Hero = APs, Header-Badge
      „Architektur", `graph TB` Banded-Layer); **Heatmap-Klick-Toggle**.
- [x] **Doku-Bildpfad-Fix** `../images` → `../../images` (auch arbeitspakete.md).
- [x] **Version 1.0.0 → 1.1.0** überall konsistent (R-VERSION/R-APPIMAGE), Gate grün.
- [x] **Offline-Release AlmaLinux 9**: `wheels/` (32 cp312/manylinux, offline-Install
      verifiziert), `tools/build_release.py`, `deploy/install.sh`, `run.sh release`.
      **GitHub-Release v1.1.0** + ZIP-Asset; Offline-Doku-Schnellstart inkl.
      **GitHub-Release-Transferweg** (ziehen → scp/USB → `install.sh`). Asset nach
      Doku-Update refreshed.
- [x] **ADR-0001** (`mpp-docs/docs/decisions/`): Native vs. Container — native bleibt
      Default für air-gapped Single-VM; **AP-11 von Docker → Podman/Quadlets**
      umgeschrieben. Roadmap/Gantt nachgezogen.
- [x] **Entscheidung Python 3.12** bleibt (AlmaLinux 9 paketiert kein 3.14).
- [x] Roadmap-Status, Changelog, KPI-Matrix, Insight nachgezogen. `main` gepusht.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (einzige unverifizierte Stelle):** `deploy/install.sh` auf einer
      **echten AlmaLinux-9-Test-VM** end-to-end durchspielen. Bisher nur das
      Wheelhouse-Offline-Resolve verifiziert (frisches venv, `--no-index`, Imports OK);
      die System-Schritte (postgres/nginx/systemd/SELinux) spiegeln die Doku, sind
      aber nicht auf Alma gelaufen.
- [ ] Optional: AP-11 umsetzen (Podman-Containerfile + Quadlet-Units, rootless,
      air-gapped) — Richtung steht in ADR-0001.
- [ ] Optional: `verify_docs.sh`-Regel, die `<img src>`-Pfadtiefe gegen `site/` prüft
      (würde die `../images`-Falle künftig rot färben).

## Entscheidungen dieser Session
- **Python bleibt 3.12** (nicht 3.14): AlmaLinux 9 paketiert nur bis 3.12; 3.14 hieße
  Source-Build → bricht die `dnf install python3.12`-Offline-Story. Revisit bei Alma 10.
- **Deployment (ADR-0001):** native systemd-Installation Default; Container optional
  via **Podman/Quadlets** (RHEL-nativ), nicht Docker-CE.
- **luDBxP** = Doku-/Release-Referenzvorlage. **Repo public** (für gh-pages Free-Plan).
- **Versionierung:** MINOR/PATCH autonom, MAJOR nur Nutzer.
- **wheels/ + release/*.zip ungetrackt** (Build-Artefakte).

## Git-Status
- Branch: `main` (== `origin/main`, gepusht), Tests **239 grün**, Doku-Gate grün.
- Letzter Code-Commit: `2783f67` (Changelog). Dieser Handoff + KPI-Update folgen.
- gh-pages live; **GitHub-Release v1.1.0** mit aktuellem Offline-ZIP.
- Stash: keine.

## Hinweise
- venv-Shebangs kaputt → Tools via `venv/bin/python3 -m …`.
- git-Remote zeigt auf alten Namen `MPP_Django` (redirected auf `lucent-job-MPP_Django`);
  Pushes/`gh` via Redirect ok (für `gh` ggf. `--repo monoeagle/lucent-job-MPP_Django`).
- Screenshot-Trick für fixed Sidebar in voller Höhe: CDP `Emulation.setDeviceMetricsOverride`
  (hoher Viewport) + **normaler** `Page.captureScreenshot` (kein `captureBeyondViewport`).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django 6 + HTMX, TDD).
Letzte Session: 2026-06-27 16:44 (v1.1.0 released, Offline-Release für AlmaLinux 9).
Kontext: Doku live auf gh-pages; Offline-Release als GitHub-Release v1.1.0; ADR-0001 (native Default, AP-11→Podman/Quadlets); Python bleibt 3.12.
Nächster Schritt: deploy/install.sh auf einer echten AlmaLinux-9-Test-VM end-to-end durchspielen (einzige unverifizierte Stelle) — Wheelhouse-Offline-Resolve ist verifiziert.
Handoff-Datei: docs/handoffs/2026-06-27-1644.md
