# Session Handoff — 2026-07-03 21:35

## Was wir gemacht haben
Kurze Session 4: Aus der Frage „was benutzen wir als Webserver?" wurde eine
**Doku-Ehrlichkeits-Korrektur**. Befund: Das Deployment (nginx + gunicorn/WSGI)
ist korrekt — aber die Doku behauptete Django Channels (WebSocket-Live-Updates),
das **nie gebaut wurde** (kein `channels` in requirements, `asgi.py` Django-Default,
kein Consumer). Doku korrigiert, **AP-12** als ehrliches Arbeitspaket angelegt,
gh-pages neu deployed. Version bleibt **v1.1.0**, Tests bleiben **239**.

## Erledigt (Session 4, Commit `67e0d5e` + Handoff-Commit)
- [x] **Webserver-Frage geklärt:** nginx (TLS, Reverse Proxy) → gunicorn/WSGI
      `127.0.0.1:8001` (3 Worker, systemd `mpp-web`) + `mpp-celery`.
- [x] **Doku-Lüge korrigiert:** `referenz/oberflaeche.md` (Benachrichtigungen
      aktualisieren per Seitenaufruf, Live via Channels = „geplant AP-12"),
      Root-`CLAUDE.md`-Stack-Zeile („Django Channels geplant, AP-12").
- [x] **AP-12 · Live-Updates via Django Channels** in `todo.md` angelegt
      (channels+channels-redis inkl. Wheelhouse, ProtocolTypeRouter,
      NotificationConsumer, Uvicorn-Worker statt `config.wsgi`,
      nginx-WS-Upgrade, TDD, DoD).
- [x] **Alle Übersichts-Flächen nachgezogen:** AP-Flowchart + Gantt (AP-12 einzeln),
      Architektur-Diagramm `index-1.mmd` (WS-Knoten gestrichelt „geplant AP-12"),
      Roadmap-Prosa, Changelog-Eintrag. Gerenderte SVGs/HTML grep-verifiziert.
- [x] **Doku-Gate grün** (R-AP-SYNC bestätigt max AP 12 beidseitig);
      **Docs-AppImage neu gebaut** (`./run.sh docs-appimage`) + nach
      `build/`/`release/`/`../AppImages/` synchronisiert.
- [x] **gh-pages deployed** (nach Nutzer-Freigabe) und live per `curl` verifiziert.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (unverändert aus Session 3, einzige unverifizierte Stelle):**
      `deploy/install.sh` auf einer **echten AlmaLinux-9-Test-VM** end-to-end
      durchspielen. Wheelhouse-Offline-Resolve ist verifiziert; die System-Schritte
      (postgres/nginx/systemd/SELinux) sind nie auf Alma gelaufen.
- [ ] Optional: **AP-11** (Podman/Quadlets, ADR-0001) oder **AP-12** (Channels,
      Schritte stehen in `todo.md`) umsetzen.
- [ ] Optional: `verify_docs.sh` um **AppImage-Frische-Regel** erweitern
      (R-APPIMAGE prüft nur Byte-Gleichheit der 3 Kopien, nicht ob die
      gebündelte Site aktuell ist) + bestehende Idee `<img src>`-Pfadtiefe-Regel.

## Entscheidungen dieser Session
- **Doku ehrlich statt Feature still nachbauen:** Falsche Ist-Behauptung sofort
  korrigieren, Zielbild als benanntes AP (AP-12) in Roadmap/Board/Gantt — nicht
  als Stack-Prosa stehen lassen.
- **Kein Version-Bump** für reine Doku-Korrektur (wie Session-2-Präzedenz:
  dated Changelog-Eintrag, kein Bump).

## Git-Status
- Branch: `main` (== `origin/main` nach Push), Tests **239 grün** (unverändert,
  kein Code angefasst), Doku-Gate grün, gh-pages aktuell.
- Letzter Code-/Doku-Commit: `67e0d5e`. Dieser Handoff + Insight + KPI-Zeile folgen.
- Stash: keine.

## Hinweise
- venv-Shebangs kaputt → Tools via `venv/bin/python3 -m …`; Docs-Venv:
  `mpp-docs/.venv-docs/bin/python3 build_docs.py`.
- Nach Doku-Änderungen **Docs-AppImage manuell** neu bauen
  (`./run.sh docs-appimage`) und nach `../AppImages/` kopieren — das Gate merkt
  veraltete Inhalte nicht (nur Byte-Gleichheit).
- git-Remote zeigt weiter auf alten Namen `MPP_Django` (Redirect ok;
  `gh` ggf. mit `--repo monoeagle/lucent-job-MPP_Django`).
- gh-pages-Deploy (`mpp-docs/deploy_ghpages.sh`) braucht explizite Nutzer-Freigabe
  (Permission-Classifier blockt Auto-Ausführung).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django 6 + HTMX, TDD).
Letzte Session: 2026-07-03 21:35 (Doku-Korrektur Channels → AP-12, gh-pages aktualisiert, v1.1.0 unverändert).
Kontext: Channels war nur Zielbild-Prosa — jetzt als AP-12 in todo.md; Deployment nginx+gunicorn/WSGI ist korrekt.
Nächster Schritt: deploy/install.sh auf einer echten AlmaLinux-9-Test-VM end-to-end durchspielen (einzige unverifizierte Stelle) — alternativ AP-11 (Podman) oder AP-12 (Channels) starten.
Handoff-Datei: docs/handoffs/2026-07-03-2135.md
