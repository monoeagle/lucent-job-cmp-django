# Session Handoff — 2026-06-27 14:54

## Was wir gemacht haben
Doku-/Release-Politur des MPP-Portals, getrieben durch das Schwesterprojekt
**luDBxP** als Vorlage: Oberflächen-Galerie + gh-pages-Veröffentlichung,
Startseite/Architektur im luDBxP-Stil, ein Bestell-Bugfix und ein **Offline-Release
für AlmaLinux 9** (gebündelte Wheels + Installer). Version auf **v1.1.0** gebumpt.

## Erledigt
- [x] **Bugfix Bestell-Flow** (`fix(orders)`): Pflicht-Parameter `location` wurde
      als Kontext-Key weggefiltert → „Parameter validation failed". TDD, 238→239.
- [x] **Oberflächen-Galerie** `mpp-docs/docs/referenz/oberflaeche.md`: 13
      Selenium-Screenshots (Requester/Approver/Admin), Lightbox. Bestellformular
      **leer + ausgefüllt side-by-side**, inline auf 560px beschnitten (Klick =
      ganzes Bild).
- [x] **gh-pages live**: Repo auf **public** gesetzt, Pages aktiviert, Deploy-Skript
      `mpp-docs/deploy_ghpages.sh`. URL: https://monoeagle.github.io/lucent-job-MPP_Django/
- [x] **Startseite/Architektur an luDBxP angeglichen**: Hero = gruppierte APs,
      Header-Badge „Architektur" (`addArchBadge`), Architektur-Diagramm als
      `graph TB` Banded-Layer (UI→Web→Service→Daten/Infra). Startseite reduziert
      (nur AP-Bild + Heatmap + Insights).
- [x] **Heatmap-Klick-Toggle**: Kachel/Phase öffnet Detailliste, erneuter Klick
      schließt sie (`activity-heatmap.js`, `closeDetail`/`activeKey`).
- [x] **Doku-Bildpfad-Fix** `../images` → `../../images` (auch latent in
      `arbeitspakete.md`).
- [x] **Version 1.0.0 → 1.1.0** überall (R-VERSION + R-APPIMAGE konsistent),
      Status-Badge 230→239, Changelog-Eintrag, Doku-Gate grün.
- [x] **Offline-Release AlmaLinux 9**: `wheels/` (32 cp312/manylinux Wheels,
      offline-Install verifiziert), `tools/build_release.py`, `deploy/install.sh`
      (idiotensicher), `run.sh release`, Schnellstart in `vm-installation-offline.md`.
      **GitHub-Release v1.1.0** mit ZIP-Asset veröffentlicht.
- [x] `main` gepusht, gh-pages gepusht.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (einzige unverifizierte Stelle):** `deploy/install.sh` auf einer
      **echten AlmaLinux-9-Test-VM** end-to-end durchspielen. Bisher nur: Wheelhouse
      offline-Resolve verifiziert (frisches venv, `--no-index`, Imports OK);
      die System-Schritte (postgres/nginx/systemd/SELinux) spiegeln die Doku, sind
      aber nicht auf Alma gelaufen. Stack/Python **3.12 bleibt** (Entscheidung).
- [ ] Optional: `verify_docs.sh` um eine Regel erweitern, die `<img src>`-Pfadtiefe
      gegen `site/` prüft (würde die `../images`-Falle künftig rot färben).
- [ ] AP-11 (Docker) weiterhin zurückgestellt (aus Session 2).

## Entscheidungen dieser Session
- **Python bleibt 3.12** (nicht 3.14): AlmaLinux 9 paketiert Python nur bis 3.12;
  3.14 hieße Source-Build auf der VM → bricht die saubere `dnf install python3.12`-
  Offline-Story. cp314-Wheels existieren zwar, aber kein OS-Paket. Revisit erst bei
  AlmaLinux 10.
- **luDBxP ist die Referenz-Vorlage** für Doku-Stil & Release-Modell.
- **Repo ist jetzt public** (nötig für gh-pages auf Free-Plan; Secret-Scan war sauber).
- **Versionierung:** MINOR/PATCH darf ich autonom bumpen, MAJOR nur der Nutzer.
- **wheels/ + release/*.zip bleiben ungetrackt** (Build-Artefakte, wie AppImages).

## Git-Status
- Branch: `main` (== `origin/main`, gepusht)
- Letzter Commit: `67960a8 docs(oberflaechen): Bestellformular-Bilder inline auf 560px beschneiden`
- Uncommitted: **handoff-Artefakte dieser Datei** (session-kennzahlen.md, insights/, handoffs/) — noch zu committen
- Stash: keine
- Tests: **239 grün**; Doku-Gate grün; gh-pages live; GitHub-Release v1.1.0 mit ZIP

## Hinweise
- venv-Shebangs kaputt → Tools via `venv/bin/python3 -m …` (nicht `pip`/Console-Scripts).
- git-Remote zeigt noch auf alten Namen `MPP_Django` (redirected auf
  `lucent-job-MPP_Django`); Pushes funktionieren via Redirect.

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django 6 + HTMX, TDD).
Letzte Session: 2026-06-27 14:54 (v1.1.0 released).
Kontext: Doku liegt live auf gh-pages, Offline-Release für AlmaLinux 9 gebaut.
Zuletzt erledigt: Oberflächen-Galerie + gh-pages + Offline-Release + Startseite/Architektur im luDBxP-Stil; Python bleibt 3.12.
Nächster Schritt: `deploy/install.sh` auf einer echten AlmaLinux-9-Test-VM end-to-end durchspielen (einzige unverifizierte Stelle) — Wheelhouse-Offline-Resolve ist schon verifiziert.
Handoff-Datei: docs/handoffs/2026-06-27-1454.md
