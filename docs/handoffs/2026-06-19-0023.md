# Session Handoff — 2026-06-19 00:23

## Was wir gemacht haben
Den ausstehenden Doku-Commit der Vorsession umgesetzt, dann **AP-11 (Docker) zurückgestellt**
und stattdessen die **Deployment-Fähigkeit** geschaffen: ein env-basiertes Production-Settings-
Modul (TDD) plus zwei detaillierte VM-Installationsanleitungen (online + offline/air-gapped).
Abschließend Doku synchronisiert (Tests 230→238) und alles committet + gepusht.

## Erledigt
- [x] **Commit-Entscheidung umgesetzt:** `.tools/` + `*.AppImage` ignoriert, `mpp-docs/site/`
      + beide AppImages (Docs + 85-MB-App) untrackt; ein Doku-Commit (`f6a7286`)
- [x] **`config.settings.production`** (env-basiert, django-environ): SECRET_KEY/ALLOWED_HOSTS/
      DATABASE_URL/Celery aus der Umgebung; Hardening → `manage.py check --deploy` 0 Issues
- [x] **TDD:** 8 Tests `tests/unit/test_production_settings.py` (RED→GREEN); Suite **238 grün**
- [x] `.env.example`, `requirements/production.txt` (gunicorn), `django-environ` in requirements
- [x] **`docs/deployment/vm-installation.md`** — Rocky/AlmaLinux 9, Voll-Produktion
      (gunicorn + nginx + systemd + PostgreSQL 16 + Redis + Let's-Encrypt-TLS + SELinux/firewalld)
- [x] **`docs/deployment/vm-installation-offline.md`** — air-gapped (Staging-Bundle,
      `dnf download --resolve --alldeps`, `pip --no-index`, internes/self-signed TLS)
- [x] README + Querverweise; Push-Divergenz via **Merge** (`1df0e44`) gelöst
- [x] **Doku-Sync (Handoff):** Tests 230→238 in testing/ueberblick/arbeitspakete + neuer
      changelog-Eintrag; Site neu gebaut (`testCount`=238), **`verify_docs.sh` exit 0**
- [x] Insight + KPI-Matrix-Zeile (Session 2) nachgezogen

## In Arbeit / Nächste Schritte
- [ ] **SOFORT:** Diese Handoff-/Doku-Sync-Änderungen committen + pushen
      (Vorschlag: `docs: Testzahlen 230→238 sync + Session-2-Handoff`).
- [ ] **AP-11 Docker-Setup** (einziges offenes AP, `todo.md`): Dockerfile (Multi-Stage) +
      docker-compose (web + PostgreSQL + Redis + Celery), Entrypoint (migrate+seed+collectstatic).
      DoD: `docker compose up` lauffähig · 238 Tests im Container grün · Doku nachgezogen.
- [ ] **Optional/offen:** (a) `origin`-Remote-URL auf neuen Repo-Namen umstellen
      (`git remote set-url origin https://github.com/monoeagle/lucent-job-MPP_Django.git`);
      (b) 85-MB-App-AppImage aus der **History** entfernen (`git filter-repo`/BFG + Force-Push),
      falls Repo-Verschlankung gewünscht.

## Entscheidungen dieser Session
- **AP-11 (Docker) zurückgestellt** zugunsten dezidierter VM-Installation (online + offline).
- **Production-Settings env-basiert** (django-environ) statt Inline-Overrides — setzt die
  Projekt-Regel „Settings via django-environ" erstmals real um.
- **Offline-TLS:** internes CA-/self-signed Zertifikat statt Let's Encrypt (ACME offline unmöglich).
- **Merge statt Rebase** bei Push-Divergenz mit untracked `site/`-Leftovers (Rebase bricht ab,
  Merge behält die Löschung) — siehe Insight 2026-06-19.
- **AppImages nicht versionieren** (`*.AppImage`), nur lokal/Build-Artefakt.

## Git-Status
- Branch: `main` (origin/main = `36a5704`; danach lokale Doku-Sync-Edits **noch uncommitted**)
- Uncommitted bei Handoff-Erstellung: mpp-docs Test-Zahlen (changelog/testing/ueberblick/
  arbeitspakete + neu gebautes `site/`), `docs/{insights,handoffs,session-kennzahlen}`
- Stash: keine

## Build / Verify
- Tests: `venv/bin/python3 -m pytest -q` → 238 passed
- Deploy-Check: `DJANGO_SETTINGS_MODULE=config.settings.production … manage.py check --deploy`
- Docs: `cd mpp-docs && .venv-docs/bin/python3 build_docs.py && ./verify_docs.sh` (exit 0)

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django + HTMX).
Letzte Session: 2026-06-19 00:23
Kontext: Deployment-fähig gemacht — env-basierte Production-Settings (TDD, 238 Tests grün) +
  zwei VM-Anleitungen (online + offline/air-gapped). Doku synchron, TDD-Gate grün.
Zuletzt erledigt: Production-Settings + VM-Deployment-Doku; Doku-Sync 230→238.
Nächster Schritt: (1) Doku-Sync-/Handoff-Änderungen committen+pushen; danach
  (2) AP-11 Docker-Setup (Dockerfile + docker-compose + Entrypoint, siehe `todo.md`).
Build/Verify: `cd mpp-docs && .venv-docs/bin/python3 build_docs.py && ./verify_docs.sh`
Handoff-Datei: docs/handoffs/2026-06-19-0023.md
