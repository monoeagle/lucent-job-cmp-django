# Session Handoff — 2026-06-18 23:22

## Was wir gemacht haben
Doku von MPP Django „gerade gezogen" und vollständig an die (mehrfach parallel erweiterten)
`.pattern` angeglichen — von einer driftenden Minimal-Doku zu einer pattern-konformen App-Look-Doku
mit **TDD-Gate**. „Fertig" heißt jetzt: `mpp-docs/verify_docs.sh` läuft mit exit 0 (12 Regeln).

## Erledigt
- [x] Test-Umgebung repariert (Django offline aus `.wheels/` ins `venv`), **230 Tests grün**
- [x] Doku-Zahlen synchronisiert (Tests 228→230, Services 9→10, E2E ~28→4)
- [x] **App-Look** komplett: JS-Suite, Voll-`extra.css`, Mermaid-Pipeline, Home-Layout, Tabs raus
- [x] `count_tests`-Bug gefixt (zählte `venv/` mit → 244; jetzt 230)
- [x] **TDD-Gate** `verify_docs.sh` (12 Regeln) — **grün**
- [x] R-HOME (Inhalt → `grundlagen/ueberblick.md`), AP-Roadmap (`todo.md`/`todo-erledigt.md` +
      `arbeitspakete.md` Flowchart+Gantt), `font=false` (kein CDN), Platzhalter raus
- [x] **§H AppRun**: Ephemeral-Port + isolierte Chromium-App (R-APPRUN), funktional verifiziert
- [x] AppImage neu, byte-gleich in `build/` = `release/` = `_Projects/AppImages/`
- [x] `todos.md` → `todo.md` + `todo-erledigt.md` konsolidiert
- [x] Insight + KPI-Matrix + Memory (`docs-tdd-gate`, `always-project-venv`, venv-shebangs) nachgezogen

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (Nutzer-Entscheidung):** Commit. Alles ist uncommitted. Vorschlag: **ein** Commit
      „docs: App-Look + TDD-Gate + Sync (verify_docs.sh grün)". Vorher `.tools/` ignorieren
      (heruntergeladenes appimagetool, 1,7 MB). Klären: getrackte `release/`-AppImage mit committen?
- [ ] **AP-11 Docker-Setup** ist das einzige offene Arbeitspaket (`todo.md`): Dockerfile +
      docker-compose (web+PostgreSQL+Redis+Celery). DoD: `docker compose up` lauffähig, 230 Tests im Container.
- [ ] **Upstream-Pattern-Fixes** (in paralleler Session): `prereqs/docs/generate_project_activity.py`
      hat denselben `venv/`-Zähl-Bug; `lucent-docs.pattern` `docs_port`-Range 8040–8099 → real 50xx.

## Entscheidungen dieser Session
- **TDD-Gate als „done"-Kriterium:** `verify_docs.sh` (12 Regeln) statt „Build lief/sieht gut aus".
- **`docs_port` 5063 bleibt:** Pattern-Range veraltet, Ökosystem nutzt 50xx.
- **`mpp-docs/site/` aus Tracking genommen** (Pattern §12) — generierte Site nicht versionieren.
- **`font=false`** statt Google-Fonts-CDN (globale No-CDN-Regel).
- **Gantt-Spannen schematisch** — Git-Historie zu v1.0.0 (2026-03-29) gestaucht, Reihenfolge real.

## Git-Status
- Branch: `main`
- Uncommitted: 11 M (Doku-Quellen, `run.sh`, `.gitignore`, `extra.css`, `build_docs.py`, `zensical.toml`),
  1 D (`todos.md`), ~14 neu (`javascripts/`, `tools/`, `arbeitspakete.md`, `ueberblick.md`,
  `verify_docs.sh`, `todo.md`, `todo-erledigt.md`, `docs/{insights,handoffs,session-kennzahlen}`),
  27 staged Deletions `mpp-docs/site/` (Untracking). `release/`-AppImage geändert.
- Stash: keine

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP Django (Marketplace Portal, Django+HTMX).
Letzte Session: 2026-06-18 23:22
Kontext: Doku ist pattern-konform + TDD-Gate grün (`mpp-docs/verify_docs.sh` exit 0, 12 Regeln).
Zuletzt erledigt: App-Look-Umbau, TDD-Gate, §H-AppRun (Ephemeral-Port + Chromium-App).
Nächster Schritt: (1) Commit-Entscheidung umsetzen (`.tools/` ignorieren, ein Doku-Commit);
  danach (2) AP-11 Docker-Setup (Dockerfile + docker-compose, siehe `todo.md`).
Build/Verify: `cd mpp-docs && .venv-docs/bin/python3 build_docs.py && ./verify_docs.sh`
Handoff-Datei: docs/handoffs/2026-06-18-2322.md
