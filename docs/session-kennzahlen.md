# Session-Kennzahlen — MPP Django

Eine Zeile pro Session (Schema aus `session-handoff-kpi.pattern`, Python-adaptiert:
`flutter test` → `venv/bin/python3 -m pytest`, `pubspec`-Version → `changelog.md`/`zensical.toml`,
`flutter analyze` → `ruff`).

| # | Datum | Modell | Tokens gesamt | Commits (Merges) | Tests (von→bis, Δ) | Version (von→bis) | Subagent-Dispatches | Features / APs | Dateien angefasst | feat/fix-Commits | Review-Bugs | Notiz |
|---|-------|--------|---------------|------------------|--------------------|-------------------|---------------------|----------------|-------------------|------------------|-------------|-------|
| 1 | 2026-06-18 | Opus 4.8 (1M) | ~310k (31% von 1M) | 0 (0) | 228→230 (+2, Doku-Korrektur; Code unverändert) | v1.0.0→v1.0.0 | 0 | Doku-Sync + **App-Look-Umbau** (JS-Suite, Mermaid-Pipeline, Home-Layout) + **TDD-Gate** `verify_docs.sh` (12 Regeln, grün) + AP-Roadmap (Flowchart/Gantt) + §H-AppRun (Ephemeral-Port/Chromium) | 26 (ohne generiertes `site/`) | 0/0 | 9 (Doku-Drift: Tests/Services/E2E/DashboardService; +Pattern-Gaps: count_tests-venv-Bug, Google-Fonts-CDN, fixer AppImage-Port, xdg-open) | Große Session über 4 Nachrichten: Sync → App-Look → TDD-Gate → §H. „Doku fertig" jetzt = Gate exit 0. Noch nicht committet. |
| 2 | 2026-06-19 | Opus 4.8 (1M) | ~205k (21% von 1M, geschätzt) | 6 (1) | 230→238 (+8, echte Code-Tests: `production.py`) | v1.0.0→v1.0.0 (dated changelog, kein Bump) | 0 | **Production-Settings** `config.settings.production` (env/django-environ, `check --deploy` 0 Issues, TDD) + **VM-Deployment-Doku** online (`vm-installation.md`, Rocky 9 Voll-Prod) **+ offline/air-gapped** (`vm-installation-offline.md`, Bundle-Transport/`pip --no-index`/internes TLS) + Repo-Hygiene (AppImages untracked) | ~17 | 1/0 | 0 (kein formaler Review) | AP-11 (Docker) zurückgestellt. Push-Konflikt via **Merge** gelöst (Rebase scheiterte an untracked `site/`-Leftovers). Offen: Repo-Umzug-URL, 85-MB-AppImage in History. |
| 3 | 2026-06-27 | Opus 4.8 (1M) | ~720k (geschätzt, sehr lange Session) | 16 (0) | 238→239 (+1, `location`-Regressionstest, TDD) | v1.0.0→**v1.1.0** (erster Bump; MINOR autonom, MAJOR bleibt Nutzer) | 0 | **Oberflächen-Galerie** (13 Selenium-Screenshots über Requester/Approver/Admin, Lightbox; Bestellformular leer/voll side-by-side, inline beschnitten) + **gh-pages-Deployment** (Repo public, Pages aktiviert, `deploy_ghpages.sh`) + **Startseite/Architektur luDBxP-Stil** (Hero=APs, Arch-Header-Badge, `graph TB` Banded-Layer) + **Heatmap-Klick-Toggle** + **`location`-Bugfix** (Kontext-Key-Kollision, TDD) + **Offline-Release AlmaLinux 9** (Wheelhouse 32 Wheels cp312/manylinux, `build_release.py`, `deploy/install.sh`, GitHub-Release v1.1.0) | ~25 | 3 feat / 1 fix (+2 chore, 2 docs) | 3 (`location`-Validierung [Nutzer via Screenshot]; Bildpfad `../images`→`../../images` [latent, auch arbeitspakete.md]; veraltete Activity-JSON) | luDBxP als Doku-/Release-Referenz. Verifikations-Lektion: Gate prüft Datei-Existenz, nicht Browser-Auflösung → alle Doku-Artefakte headless im echten Browser gegengeprüft. Entscheidung: **Python 3.12 bleibt** (AlmaLinux 9 paketiert kein 3.14). Forts. nach 1. Handoff: **ADR-0001** (Native-vs-Container, AP-11 → Podman/Quadlets), Bestellformular-Vergleichsbilder (gleich groß, volle Zusammenfassung via CDP-Viewport), Offline-Doku um GitHub-Release-Transferweg + Release-Asset refreshed. Offen/ehrlich: `deploy/install.sh` nicht auf echter Alma-VM end-to-end getestet. |
| 4 | 2026-07-03 | Fable 5 | ~80k (geschätzt, kurze Session) | 4 (0) | 239→239 (±0, kein Code angefasst) | v1.1.0→v1.1.0 (dated Changelog, kein Bump) | 0 | **Doku-Ehrlichkeits-Korrektur Channels**: Webserver-Frage → Befund, dass Django Channels nie gebaut wurde (Doku behauptete Live-WebSocket; kein `channels` in requirements, `asgi.py` Default). `oberflaeche.md`/`CLAUDE.md` korrigiert, **AP-12 · Live-Updates (Channels)** in `todo.md` + Flowchart/Gantt/Architektur-Diagramm („geplant" gestrichelt), Changelog-Eintrag, Docs-AppImage neu gebaut, **gh-pages deployed** (Live-Site per `curl` verifiziert) | 12 (+ Handoff/Insight/KPI) | 0/0 (2 docs) | 3 (Doku-Drift „Channels live" [selbst gefunden]; Gate-Lücke: R-APPIMAGE prüft keine Inhalts-Frische; `deploy_ghpages.sh --no-activity` frischt Heatmap nie auf [Nutzer bemerkte fehlende Aktualisierung]) | Lektion: Doku/Code-Widerspruch erst auflösen — auch bestehende Referenz-Prosa kann Zielbild sein (Umkehrung der Spec-≠-Ist-Regel); Zielbild-Features als benanntes AP, nie als Ist-Behauptung. gh-pages-Deploy braucht explizite Nutzer-Freigabe. Offen unverändert: `install.sh` auf echter Alma-9-VM. |
| 5 | 2026-07-16 | Opus 4.8 (1M) | ~500k (geschätzt, sehr lange Session) | 8 (0) | 239→317 (+78, Installer-Logik TDD: lib.sh/ui.sh) | v1.1.0→**v1.2.0** (MINOR autonom; Release veröffentlicht) | 0 | **Offline-Installer produktionsreif**: idempotent (SECRET_KEY bleibt, Rolle/DB getrennt geprüft, App gespiegelt statt gemerged, `restart` statt `enable --now`), **PostgreSQL-Erkennung PGDG+AppStream** (psql-Pfad/Service-Name/initdb je Variante), `--with-packages`/`--skip-nginx`/`--install`/`--check`/`--restart`, **Menü mit Prüfbereich + Links/Ports** (`deploy/ui.sh`, Erhebung vom Rendering getrennt), `VERSION` ins Bundle, **R-STALE repariert** (erhebt Wahrheit per `pytest --collect-only` statt Alt-Konstanten, prüft HTML+JS), Referenz-Seite **Architektur-Vergleich** (SSR vs. API-First) | 28 (ohne generiertes `site/`; +2811/−202 LOC) | 1 feat / 2 fix (+2 docs, 1 chore, 1 release) | 17 (11 Installer-Bugs: `enable --now` startet nicht neu [stiller Upgrade-Fehler], DB-Anlage an Rollen-Existenz gekoppelt, SECRET_KEY-Rotation, `cp -a` löscht nie, Env-Quoting bei Leerzeichen, psql über PATH statt `/usr/pgsql-16/bin/`, `postgresql-16.service` hart verdrahtet, `Requires=`-Drift Doku↔Skript, nginx nie geprüft [ChatGPT-Review], Redis nur bemängelt, `_mpp_ui_pad` bricht unter `set -e` ab [Test-Fund]; 5 Doku/Gate-Drifts: Badge 239 + Info-Tabelle 230 hartkodiert, R-STALE strukturell blind, `testing.md` 238 seit 19.06., `projektstruktur.md` ohne `deploy/`+`tools/` seit v1.1.0, `VERSION` fehlte im Bundle; **`run.sh` hatte APP_VERSION hartkodiert** → AppImage wurde nach dem Bump weiter als 1.1.0 gebaut, R-VERSION prüfte diese 4. Stelle nicht) | Lektion: **Testbarkeit herstellen IST die Fehlersuche** — 2 von 3 Lese-Hypothesen waren falsch (`cp -a`-Verschachtelung widerlegt; echter Fehler war fehlendes Löschen). Tests müssen mit `set -euo pipefail` laufen wie das Ziel, sonst bleibt der schwerste Bug unsichtbar. Vakuum-grüne Tests per Mutation gegengeprüft (1 Blindgänger entlarvt). Gates driften selbst. Offen unverändert: **`install.sh` nie auf echter Alma-9-VM** — `--check` macht diesen Test jetzt billiger. **Release v1.2.0** veröffentlicht (ZIP nach dem Upload wieder heruntergeladen + verifiziert; Installer aus dem entpackten Bundle heraus getestet). Zweite Gate-Lücke derselben Klasse: R-VERSION kannte `run.sh` nicht → behoben + Version wird jetzt aus `lucent-hub.yml` abgeleitet statt gespiegelt. Offen: **gh-pages hängt auf v1.1.0**. |
| 6 | 2026-07-17 | Opus 4.8 (1M) | ~580k (58% von 1M, geschätzt; sehr lange Session, 8 Nachrichten) | 8 (1 FF-Merge nach `main`) | 317→330 (+13, HTTP-Modus/Redis-RPM/Panel-Breite TDD) | v1.2.0→**v1.3.1** (MINOR autonom) | 0 | **Rename MPP→CloudMan Portal (CMP)** komplett (Container `mpp/`→`cmp/`, 31 `cmp_*`-Funktionen, System-Identifier, Test/Dev-DB, `cmp_tags`, Celery-App, Branding, Doku-Site `cmp-docs/`, scripts) + **HTTP/HTTPS-Auto-Modus** (`production.py` Secure-Cookies env-steuerbar, `cmp_render_nginx`/`cmp_env_security_lines`/`cmp_portal_proto`) + **Redis air-gapped** aus Bundle-`rpms/` + **Doku-HTML-ZIP** (`./run.sh docs-zip`, Gate R-APPIMAGE→R-DOCS-ZIP) + **v1.3.0-Release** (Version synchron, Changelog, Kennzahlen) | ~190 (168 Rename inkl. 390 git-Renames + Features/Doku) | 3 feat / 0 fix (+3 refactor, 2 docs) | 5 selbst gefunden (kein formaler Review): `scripts/` funktional kaputt nach `git mv` [MPP_DIR ins Leere]; `seed.py`+`todo-erledigt.md` auf v1.1.0 — R-VERSION prüft sie nicht [via Memory-Policy gefunden]; `ueberblick.md` Tests 238 Alt-Drift; HTTP-Blockade **zweite versteckte Ursache** [harte Secure-Cookies, am Code gelesen statt geraten]; Testzahl 3× nachzuziehen [317→326→328] | Rename fiel an den Rändern, nicht im Kern (Django-Code importfrei von `mpp`) — Gefahr an `mpp-TDD`-Schwesterprojekt, Repo-URLs, agent-memory-Pfaden, Cross-Projekt-`fix_databases.sh`; `sed` mit Masking + grep-Beleg. **Kein Migrationsskript** (Nutzer: cmp frisch aufsetzen). Test/Dev-DB real umbenannt (Nutzer musste 1 sudo-psql-Befehl selbst ausführen). v1.3.0 nach `main` gemergt+gepusht, **kein Tag/Release erstellt**. Offen: gh-pages (jetzt auf v1.1.0), VM-Verifikation (nie auf echter Hardware), GitHub-Repo-Rename. |

## Ableitbare KPIs (über die Zeilen — ab Session 2 sinnvoll)

- Tokens/Commit, Tests-Δ je 100k Tokens, Doc-vs-Code-LOC-Anteil
- Fix-vs-Feat-Quote (Rework-Anteil), Fang-Quote (Review-Bugs / (Review + Laufzeit-Bugs))
- Code-Health-Trend: `ruff`-Issues von→bis je Session
| 7 | 2026-07-20 | Opus 4.8 (1M) | ~120k (12% von 1M, geschätzt; 12 Nachrichten, kein Subagent) | 3 (+1 Handoff-Commit; direkt auf `main`) | 330→330 (±0, kein Code geändert) | v1.3.1→**v1.3.2** (PATCH autonom, reines Doku-Release) | 0 | **Neue Doku-Seite „Betrieb → Laufzeit-Topologie"** (3 Mermaid: Produktion/Einstiegspunkte-nach-Rolle/Entwicklung; Ports-Tabelle, Dienst-Aufgaben mit Begründung, TLS-Modus-Matrix, Client-Zugriff FQDN/hosts/CSRF) + 2 Build-Fixes (`.nojekyll` überlebt Build; R-STALE prüft wieder) + Querverweise Architektur→Laufzeit | 47 (überwiegend `site/`-Rebuild; ~10 Quellen) | 0 feat / 2 fix (+2 docs, 1 release) | 3 eigene Fehlannahmen je in 1 Befehl widerlegt: „v1.3.1 nicht getaggt" [Tag existierte remote, lokal nur nicht gefetcht]; „R-STALE scheitert an venv-Shebangs" [real: cwd — `pytest.ini` liegt im Root, aus `cmp-docs/` 0 Tests gesammelt]; „`admin`-Rolle und `is_staff` entkoppelt" [`seed_stub_users()` koppelt sie sehr wohl] · 1 stiller Deploy-Bug gefunden (`site/.nojekyll` vom Build gelöscht → `_data/` auf gh-pages tot, ohne Fehlermeldung) | **Doku-Gate erstmals wieder 12/12 grün** — R-STALE war seit unbekannt dauerhaft rot und wurde als „Werkzeugproblem" hingenommen statt diagnostiziert. Kein Anwendungscode: App-Artefakt v1.3.1 bleibt gültig, nur Docs-ZIP neu. Nutzer-Missverständnis Mitte der Session (Rückfrage nach Podman/Dev-Loop-Strategie, gemeint war schlicht „Portal von einer Testmaschine aus aufrufen") — Antwort lag bereits vollständig im Repo (`install.sh` ist dasselbe Skript für Test+Prod). **Offen weiterhin: VM-Verifikation** — die ganze Topologie-Doku ist aus dem Code abgeleitet, nie auf echter Hardware beobachtet. |
| 8 | 2026-07-21 | Opus 4.8 (1M) | ~200k (20% von 1M, geschätzt; ~20 Nachrichten, kein Subagent) | 3 (2 + Handoff-Commit; direkt auf `main`, davon 1 **History-Rewrite** mit Force-Push) | 330→330 (±0, kein Anwendungscode geändert) | v1.3.2→**v1.3.3** (PATCH autonom, reines Doku-Release; **kein Tag gesetzt** — AP-13-Entscheidung offen) | 0 | **Analyse einer Fremddoku** (Bookstack-Buch der Zielumgebung, 2819 Z., API-First/DRF) gegen CMP (rein SSR): Gap-Analyse aller FM_*/FK_*-Anforderungen + Domänenmodell mit Beleg-Spalte, Antworten auf alle 19 Punkte aus `anforderungen.md`, `django.png`-Lehrplanbewertung, Prototyp-Bewertung → 693-Zeilen-Report + interne Doku-Seite unter „Intern" (ZIP-Ausschluss `intern/` + `intern-*`-Assets) | 43 (16 Quellen, Rest `site/`-Rebuild) | 0 feat / 0 fix (2 docs/release + 1 Handoff) | **1 Architektur-Bruchstelle im eigenen Code gefunden** (Workflow hinter der Genehmigung nicht verdrahtet: Celery-Tasks ruft niemand, `create_from_order` nur Tests, `AuditService.log`/`NotificationService.create` nur `seed.py` → Audit-Log + Benachrichtigungen im Betrieb nur Seed-Daten) · **1 aktiver HTMX-Fehler** (`AuditLogListView` ohne htmx-Zweig → Vollseite in `#audit-table`) · **1 Datenschutz-Fund** (angeblich anonymisierte Fremddoku enthielt AD-Kennung, Klarname, Admin-Konto, Org-Einheit — lag bereits öffentlich) · 2 eigene Fehler beim Nachmessen gefunden (CRLF→LF still normalisiert: Diff 2675 statt 7 Zeilen; Mermaid-Bildpfad `../` statt `../../` bei Tiefe-2-Seite) | Erste Session mit **Fremd-Input als Hauptgegenstand**. Der wichtigste Befund kam nicht aus der Fremddoku, sondern aus dem Zwang, jede Anforderung **grep-belegt** gegen echten Code zu prüfen — 330 grüne Tests hatten die fehlende Verdrahtung verdeckt, weil der E2E-Test die Services selbst nacheinander aufruft und damit den fehlenden Produktionscode ersetzt. Force-Push entfernt den alten Commit **nicht** (per `gh api` gegengeprüft: über SHA weiter abrufbar) → GitHub-Support-Ticket offen. Doku-Drift in `projektstruktur.md`/`arbeitspakete.md` beim Handoff-Check gefunden und sofort behoben. |
